@model RewardsForYou.Models.ViewModel

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Mission.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mission.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mission.ExpiryDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mission.Points)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mission.Finished)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.Mission) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExpiryDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Points)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Finished)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TaskID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TaskID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TaskID })
        </td>
    </tr>
}

</table>
